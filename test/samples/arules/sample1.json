{
    "rule-name": "Rule 1",
    "salience": 100,
    "when": [
        { "?test": "'hello world. People!'"},
        { "?value": "6 * 6" },

        { "assert": "exists[User.age > ?value] and exists[User.name == ?test]" }
    ],
    "then": [
        {"read": "Some value"}
    ]
}