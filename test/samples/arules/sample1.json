{
    "rule-name": "Rule 1",
    "salience": 100,
    "when": [
        { "?age": "User.id"},
        { "?age_check1": "?age < 45"},
        { "?age_check2": "not (User.email != User.name)"},

        { "assert": "exists[?age_check1 and ?age_check2] or exists[?age_check2]"}
    ],
    "then": [
        { "read": "Some value" }
    ]
}